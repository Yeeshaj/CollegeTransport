<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.dellpc.collegetransport">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.CALL_PHONE"></uses-permission>
    <uses-permission android:name="android.permission.SEND_SMS"></uses-permission>

    <application
        android:allowBackup="true"
        android:icon="@drawable/backg"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginAdmin" />
        <activity android:name=".Registration">

        </activity>
        <activity android:name=".splash" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity android:name=".busRegistration" />
        <activity android:name=".studentRegistration" />
        <activity android:name=".viewBus" />
        <activity android:name=".driverRegitration" />
        <activity android:name=".viewStudent" />
        <activity android:name=".BusAllotment" />
        <activity android:name=".update" />
        <activity android:name=".delete" />
        <activity android:name=".ViewStudentDetailsMainActivity" />
        <activity android:name=".ViewDriverDetailsMainActivity" />
    </application>

</manifest>